<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>Account</title>
</head>
<body>
    <header>
      <a href="/">HOME</a>   
    </header>
    <a href="/user/upload">Upload</a>
    <!-- アップロードへ飛ばす -->
    
   
    <p><%= @user.mail %>でログインしています</p>
    <!--　ログインしてるアカウントのEmailを出す -->
    <h1>カテゴリ一覧</h1>
    <ul>
        <% @categories.each do |category| %>
            <li>
                <a href="/user/category/<%= category %>"><%= category %></a>
            </li>
        <% end %>
    </ul>
    
     <!-- ここでカテゴリー出す -->
    <h1>動画一覧</h1>
     <ul>
        <% @contents.each do |content| %>
        <li>
            <iframe width="560" height="315" src="https://www.youtube.com/embed/<%= content.url %>" frameborder="0" allowfullscreen></iframe>
            <!-- 本当はURLから動画番号引っ張ってきて表示させたい -->
            <p><%= content.title %></p>
            <p><%= content.body %></p>
            <p><%= content.category %></p>
           
            <span>
                <form action="/user/delete/<%= content.id %>" method="post">
                    <input type="submit" value="削除">
                </form>
            </span>
            <span>
                <a href="/user/edit/<%= content.id %>">編集</a>
            </span>
        </li>
        <% end %>
    </ul>
</body>